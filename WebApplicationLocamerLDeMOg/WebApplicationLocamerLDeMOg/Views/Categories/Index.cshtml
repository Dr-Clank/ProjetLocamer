@model IEnumerable<WebApplicationLocamerLDeMOg.Models.Categorie>

@{
    ViewBag.Title = "Index";
}

<h1>Application de Gestion Locamer</h1>

<h2>Index</h2>

<p>
    @Html.ActionLink("Créer une nouvelle option", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Libellé catégorie
        </th>
        <th>
            Coéfficient catégorie
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelleCate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coefCate)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.idCate }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.idCate })
        </td>
    </tr>
}

</table>
